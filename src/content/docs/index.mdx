---
title: BoringStack
description: Postgres, HTTP, React, and Compose wired into a SaaS spine you can run locally. Fast feedback from Bun and Vite, lint as the contract for humans and agents.
template: splash
hero:
  tagline: Boring means it just works. Postgres, HTTP, React, and Compose, wired together so you ship product instead of reinventing the spine. Developer experience matters. Bun and Vite keep feedback tight, the API and SPA stay separate jobs, and ESLint carries the architecture so humans and agents get errors they can fix, not prose to interpret.
  actions:
    - text: Why BoringStack
      link: /architecture/why-boringstack/
      icon: right-arrow
      variant: primary
    - text: Quickstart
      link: /quickstart/
      icon: right-arrow
      variant: minimal
    - text: View on GitHub
      link: https://github.com/AI-Starter-Templates
      icon: external
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

Three sibling templates (API, UI, infra) come from codebases that have shipped for years. Each layer does one job. You get Bun on the API, Vite on the UI, and Compose for the spine, plus ESLint rules you can read in the repo instead of guessing from markdown alone.

[Lint as the contract](/architecture/lint-as-contract/) is where the agent story lives: same rules as humans, same `validate` gate.

Read [Why BoringStack](/architecture/why-boringstack/) for the details, then [Quickstart](/quickstart/) when you want to clone.

## The templates

<CardGrid>
  <Card title="api-template" icon="rocket">
    Bun · Elysia · Drizzle · Postgres · Valkey · BullMQ. Cookie auth with
    refresh sessions, OAuth, Stripe billing, email, queues, audit log, and an
    env validator that fails the boot rather than your customers.
    [Overview →](/api/overview/)
  </Card>
  <Card title="ui-template" icon="laptop">
    Vite · React 19 · TanStack Query · shadcn/ui · Playwright. Typed OpenAPI
    client, custom ESLint rules that enforce the architecture, working e2e
    harness. Drift between server and client is a compile error.
    [Overview →](/ui/overview/)
  </Card>
  <Card title="infra-docker-compose-template" icon="setting">
    Postgres · Valkey · Traefik. Opt-in overlays for GlitchTip,
    Prometheus + Grafana + Loki, BullMQ dashboard, WUD, and Mailpit. One
    `./dev.sh up -d` boots the lot. [Overview →](/infra/overview/)
  </Card>
  <Card title="infra-bootstrap-tofu-template" icon="seti:terraform">
    Optional fourth template. One `tofu apply` puts a Hetzner VPS behind
    Cloudflare with the docker-compose stack already running.
    [Walkthrough →](/topics/provisioning-with-tofu/)
  </Card>
</CardGrid>

## Where next

- New here? Start with the [Quickstart](/quickstart/) — TL;DR + "Use this template" + boot.
- Deploying to a VPS? [Deployment](/topics/deployment/), [Firewall & TLS](/runbooks/firewall-and-tls/), [Cloudflare Email setup](/runbooks/cloudflare-email-setup/).
- Looking up an env var or a command? [Environment variables](/reference/env-vars/) · [Commands cheatsheet](/reference/commands/).
