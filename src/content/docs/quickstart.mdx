---
title: Quickstart
description: Fork the API, UI, and infra templates, boot the local stack, sign in. About five minutes from zero to a working full-stack on your laptop.
---

import CommandRun from "../../components/docs-kit/CommandRun";
import DocCallout from "../../components/DocCallout.tsx";
import DocFileTree from "../../components/DocFileTree";
import PageIntro from "../../components/docs-kit/PageIntro";
import SignalGrid from "../../components/docs-kit/SignalGrid";
import FaqGroup from "../../components/FaqGroup.tsx";
import FaqItem from "../../components/FaqItem.tsx";

<PageIntro
  eyebrow="From empty folder to product spine"
  actions={[
    { label: "Boot the stack", href: "#step-3-boot-the-local-stack" },
    { label: "See what runs", href: "#whats-running" },
  ]}
  facts={[
    { value: "3", label: "template repos" },
    { value: "5 min", label: "local boot path" },
    { value: "0", label: "local Node or Bun installs" },
  ]}
>
  Fork the API, UI, and infra templates as siblings, let Compose wire the local
  runtime, then sign in to a real app shell. This path proves the whole spine:
  Postgres, Valkey, API, UI, generated client, hot reload, and optional [overlays](/reference/glossary#overlay).
</PageIntro>

<DocCallout
  eyebrow="At the end of this page"
  title="You'll have all of this running locally"
  variant="tip"
>
  - A working dashboard at **`http://localhost:3001`** with sign-up, login, and JWT cookies.
  - A typed **API** at `http://localhost:3000/api/*` with auto-generated OpenAPI docs at `/swagger`.
  - **Postgres** with migrations applied and **Valkey** ready for cache + queues.
  - A **generated TypeScript client** in the UI that fails to compile when the API contract drifts.
  - Optional one-flag **overlays**: Mailpit, Bull Board, observability, error tracking, image-update notifications.
  - Zero local Node, Bun, or Postgres installs &mdash; Compose owns every runtime piece.
</DocCallout>

## TL;DR

<SignalGrid
  columns={4}
  items={[
    {
      label: "01",
      title: "Use the templates",
      body: "Create API, UI, and infra repos under your org from the GitHub templates.",
    },
    {
      label: "02",
      title: "Clone as siblings",
      body: "Keep the app repos beside the infra repo so Compose build contexts stay obvious.",
    },
    {
      label: "03",
      title: "Boot Compose",
      body: "Seed the env, run the compose script, and let migrations prepare the API.",
    },
    {
      label: "04",
      title: "Sign in",
      body: "Open the UI, create the first account, and start product work from a real shell.",
    },
  ]}
/>

You now have Postgres, Valkey, the API, and the UI running locally with hot reload. The optional OpenTofu repo is only needed when you want repeatable first-time VPS provisioning.

## Prerequisites

- Docker + Docker Compose v2 (`docker compose version` reports `v2.x` or newer).
- About 4 GB of free RAM.
- No local Node or Bun needed; everything runs in containers.

## Step 1. Use the templates

BoringStack's local development path needs three GitHub template repos. On each repo, click **"Use this template"**, then **"Create a new repository"**:

- [`api-template`](https://github.com/AI-Starter-Templates/api-template), e.g. `acme-api`
- [`ui-template`](https://github.com/AI-Starter-Templates/ui-template), e.g. `acme-ui`
- [`infra-docker-compose-template`](https://github.com/AI-Starter-Templates/infra-docker-compose-template), e.g. `acme-infra`

Fork [`infra-bootstrap-tofu-template`](https://github.com/AI-Starter-Templates/infra-bootstrap-tofu-template) later if you want `tofu apply` to provision your first VPS.

Keep the canonical names if you want. Nothing breaks either way.

<DocCallout variant="note" title="Skip the docs repo" eyebrow="Template boundary">
The docs site source (`.github` repo) is **not** a template. It's BoringStack-specific content. Skip it.
</DocCallout>

## Step 2. Clone your forks as siblings

The compose build contexts use relative paths (`../../api-template`, `../../ui-template`). Whatever you named your forks, lay them out as siblings under one parent:

```bash
mkdir acme && cd acme

git clone https://github.com/<you>/acme-api
git clone https://github.com/<you>/acme-ui
git clone https://github.com/<you>/acme-infra
# Optional, for later VPS provisioning:
git clone https://github.com/<you>/acme-bootstrap
```

<DocFileTree
  root="acme/"
  title="Sibling forks"
  caption="the infra repo expects nearby app repos"
  nodes={[
    { name: "acme-api/", detail: "your fork of api-template" },
    { name: "acme-ui/", detail: "your fork of ui-template" },
    { name: "acme-infra/", detail: "your fork of infra-docker-compose-template" },
    { name: "acme-bootstrap/", detail: "optional fork of infra-bootstrap-tofu-template" },
  ]}
/>

If you renamed `api-template` or `ui-template`, either match the cloned directory name to what `compose/docker-compose.yml` expects, or edit the `build: context:` lines once.

<DocCallout variant="tip" title="Optional: setup.sh" eyebrow="Fast path">
The workspace root ships a `setup.sh` that bootstraps `compose/.env`, generates a `GLITCHTIP_SECRET_KEY`, and (with `--up`) boots the stack. Skip if you prefer doing the steps by hand.
</DocCallout>

## Step 3. Boot the local stack

<CommandRun
  title="Boot locally"
  caption="local production-like spine"
  command={[
    "cd acme-infra",
    "cp compose/.env.example compose/.env",
    "chmod +x compose/dev.sh scripts/*.sh",
    "./scripts/compose-up.sh up -d --build",
  ]}
  output={[
    { tone: "ok", text: "postgres ready on 5432" },
    { tone: "ok", text: "valkey ready on 6379" },
    { tone: "ok", text: "api-dev migrated and serving /openapi.json" },
    { tone: "ok", text: "ui-dev generated client and started Vite" },
  ]}
/>

First boot pulls base images, builds the api/ui dev images, and runs migrations. If you set `SUPERUSER_EMAIL` + `SUPERUSER_PASSWORD` in `compose/.env`, an admin user is also created. About 3 minutes on a fast laptop.

## Step 4. Sign in

Open **http://localhost:3001**.

If you set `SUPERUSER_EMAIL` + `SUPERUSER_PASSWORD` in `compose/.env` before booting, sign in with those credentials. Otherwise hit "Sign up" on the form and register a new account; that user is the first user in the system. You land on the dashboard, which is intentionally minimal. Start building from here.

## What's running

<FaqGroup>
  <FaqItem title="postgres (port 5432)" open>
    App database; schemas `auth`, `billing`, `audit`, `app`, `notifications`.
  </FaqItem>
  <FaqItem title="valkey (port 6379)">
    Cache and BullMQ queues.
  </FaqItem>
  <FaqItem title="api-migrate (one-shot)">
    `db:push` and optional superuser seed, then exits.
  </FaqItem>
  <FaqItem title="api-dev (port 3000)">
    Bun + Elysia API with hot reload via bind mount.
  </FaqItem>
  <FaqItem title="ui-dev (port 3001)">
    Vite dev server; proxies `/api/*` to `api-dev`.
  </FaqItem>
</FaqGroup>

Traefik runs only in the prod profile. In prod it terminates TLS and path-routes `/api/*` + `/health` to the api container, everything else to ui, on one domain.

## Common next steps

- Observability: `WITH_OBSERVABILITY=1 ./scripts/compose-up.sh`. [Details](/topics/observability/).
- Self-hosted error tracking: `WITH_GLITCHTIP=1 ./scripts/compose-up.sh`. [Details](/topics/error-tracking/).
- Local email testing: `WITH_MAILPIT=1 ./scripts/compose-up.sh`. [Details](/topics/email-in-dev/).
- Production deploy: [Deployment](/topics/deployment/) when you are ready for GHCR images, TLS, firewall, and backups.

## Stop the stack

<FaqGroup>
  <FaqItem title="Stop, keep data" open>
    `./scripts/compose-down.sh`. Volumes stay on disk.
  </FaqItem>
  <FaqItem title="Stop and wipe data">
    `./scripts/compose-down-clean.sh`. Prompts before delete; `CONFIRM=yes` to skip the prompt.
  </FaqItem>
</FaqGroup>

## Related

- [Why BoringStack](/architecture/why-boringstack/). What ships in the box.
- [Repository layout](/architecture/three-repos/). Why the templates stay separate.
- [Environment variables](/reference/env-vars/). Every var, what it does.
- [Deployment](/topics/deployment/). Production runtime path.
- [Provisioning with OpenTofu](/topics/provisioning-with-tofu/). `tofu apply` to a live VPS.
