---
title: Secrets
description: How env-based secrets flow from `compose/.env` into containers, and how to rotate them.
---

import { Aside } from "@astrojs/starlight/components";

Secrets live in `compose/.env` (gitignored) and are passed to containers via `env_file:` references. The template does not ship Docker secrets or Vault integration — for a single-host stack, env files + filesystem permissions is the pragmatic floor.

<Aside type="note" title="Coming soon">
  Full reference will cover: bootstrapping `.env` from `.env.example`,
  rotation playbooks (Postgres password, JWT secret, OAuth secrets,
  Cloudflare token), what to do on a leak, and when to graduate to a
  proper secret manager.

  Current source-of-truth: `compose/.env.example` in the
  [infra-docker-compose-template
  repo](https://github.com/AI-Starter-Templates/infra-docker-compose-template/blob/main/compose/.env.example).
</Aside>
