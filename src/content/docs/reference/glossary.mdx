---
title: Glossary
description: 'Project-specific terms: what we mean by "overlay", "resource", "module boundary", and friends.'
---

import { Aside } from "@astrojs/starlight/components";

A small project-specific dictionary so newcomers can decode the BoringStack vocabulary quickly.

<Aside type="note" title="Coming soon">
  Will cover terms like: **overlay** (an opt-in docker-compose file),
  **resource** (a route/service/types triple in the api-template),
  **module boundary** (the ESLint rule preventing cross-feature
  imports), **profile** (compose profile), **stack** (`dev` vs.
  `prod`), and the OpenAPI **contract** between API and UI.

  In the meantime, each term is explained at first use in the
  Architecture and Template sections.
</Aside>
